import { Background, Controls } from "@xyflow/react";

const BaseNode = () => {
  return (
    <>
      <Background />
      <Controls className="right-0 left-[unset]!" />
    </>
  );
};

export default BaseNode;
